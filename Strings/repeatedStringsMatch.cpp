#include<bits/stdc++.h>
using namespace std;

class Solution {
public:
    int repeatedStringMatch(string a, string b) {
        sort(a.begin(), a.end());
        sort(b.begin(), b.end());
        if(a==b) return 1;
    }
};

int main(){

    return 0;
}